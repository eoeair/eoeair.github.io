---
.title = "公共服务目录",
.date = @date("2025-07-20T00:00:00"),
.author = "ben0i0d",
.layout = "blog.shtml",
.alternatives = [{ 
    .name = "rss",
    .layout = "blog.xml", 
    .output = "index.xml",
}],
.draft = false,
---


# 目录
1. IaaS
2. PaaS
3. SaaS

# IaaS

## 计算设施

* 访问：https://eoelab.org:1024

* 注意：
  1. 你必须先联系infra团队成员上传客户端证书
  2. 不支持跨CPU架构，目前仅支持AMD64架构
  3. amd64架构主机的主机网络ip为`192.168.2.182`



# PaaS

由于incus支持vm、lxc、oci同层调度，k8s/potainer等平台被取消

# SaaS

## 开发服务

* github自托管runner,挂靠在eoeair组织下
  * 使用方法：GitHub Actions
  * 注意：
    1. runner基于k3s上`Actions Runner Controller`构建，是一个更好的零信任实现
    2. runner `label`为`arc`，默认runner由此提供
    3. 默认启用了`Kubernetes`模式，您的作业强制要求在一个容器内运行，极少数任务可能无法执行，请与pkg团队沟通

  * cenv：
    * 使用方法：自行拉取cenv项目打包的镜像，可以本地运行，也可以在incus创建
    * 注意：
      1. 我们要求远程开发（本地/云）都必须使用本项目来保证开发环境一致
      2. 在incus/云创建oci容器后，数据上传的流程是：`配置ssh端口出口（防火墙/proxy）` -> `service ssh start` -> `复制公钥` -> `scp`
      3. 我们要求CI/CD或运行时都必须使用本项目来保证安全和可迭代化
      4. gpu开发环境不再被支持,请遵守技术栈相关要求

## 计算服务

* Jupyterlab：
  * 使用方法：自行拉取jupyter项目打包的镜像，可以本地运行，也可以在incus创建
  * 注意：
    1. gpu开发环境不再被支持,请遵守技术栈相关要求

## MCSM

* 支持游戏类型:Minecraft（Java/基岩版，全版本支持）,steam
* 访问方式： 加入QQ群：945537763

## Nextcloud网盘

* 访问方式： https://eoelab.org:1025 , 使用GitHub登陆即可